import { Text } from '@chakra-ui/react';
import { Children } from 'react';

interface IHeading extends Text {}

const Heading = (props: IHeading) => {
  //   return <Text>{props.children}</Text>;
};

export default Heading;
